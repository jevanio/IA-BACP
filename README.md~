# IA-BACP
Proyecto de IA, resolver BACP mediante algoritmo Greedy y Tabú Search

Instrucciones de uso:
1) En proyecto.c, l4 se define la ubicación del archivo de la instancia. Indicar del siguiente modo:
	"#define Instancia "instancias/bacp8.txt""
2) Compilary ejecutar mediante makefile.
